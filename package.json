{
  "name": "magi-token-sdk-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r --filter './packages/*' run build",
    "test": "pnpm -r --filter './packages/*' run test",
    "typecheck": "pnpm -r --filter './packages/*' --filter './examples/*' run typecheck",
    "demo": "pnpm --filter demo run dev"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsup": "^8.3.5",
    "vitest": "^2.1.8"
  },
  "pnpm": {
    "overrides": {
      "@vsc.eco/nft-core": "workspace:*",
      "@vsc.eco/token-sdk": "workspace:*",
      "@vsc.eco/nft-widget": "workspace:*"
    }
  },
  "packageManager": "pnpm@10.33.0"
}
